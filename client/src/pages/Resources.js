// Setting up a resource page - this would be where we have a list of city information and links
import React, { ueState } from "React";

// Import Mutations

// Import Auth
import auth from "../utils/auth";
// Set up Resource page
const Resource = () => {};

export default Resource;
